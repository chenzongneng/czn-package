<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ 广州丰石科技有限公司拥有本软件版权2017并保留所有权利。
  ~ Copyright 2017, Guangzhou Rich Stone Data Technologies Company Limited,
  ~ All rights reserved.
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.richstonedt.garnet.modules.sys.dao.SysUserRoleDao">

	<select id="queryList" resultType="com.richstonedt.garnet.modules.sys.entity.SysUserRoleEntity">
		select * from gar_sys_user_role 
		<where>
			<if test="userId != null and userId != ''">
				user_id = #{userId}
			</if>
		</where>
	</select>
 
	<insert id="save">
		insert into gar_sys_user_role
		(
			user_id, 
			role_id
		)
		values
		<foreach collection="roleIdList" item="item" index="index" separator="," >
		(
			#{userId}, 
			#{item}
		)
		</foreach>
	</insert>
	
	<delete id="delete">
		delete from gar_sys_user_role where user_id = #{value}
	</delete>
	
	<select id="queryRoleIdList" resultType="long">
		select role_id from gar_sys_user_role where user_id = #{value}
	</select>
</mapper>